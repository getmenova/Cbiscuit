#
include < stdio.h >
  int main() {
    int number = 20;
    while (number > 10) {
      printf(“the number is % d”, number);
      number--;
    }
    return 0;
  }
  
  //print num , if less than 10 decrement divisor